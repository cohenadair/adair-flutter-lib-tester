name: adair_flutter_lib_tester
description: "A project to test Adair Flutter Lib functionality."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  adair_flutter_lib:
    # ignore: invalid_dependency
    path: ../adair-flutter-lib
  firebase_auth: ^5.7.0
  firebase_core: ^3.15.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
